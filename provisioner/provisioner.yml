---
db:
  host: 127.0.0.1
  port: 3306
  database: provisioner
  user: provisioner
  # password is intentionally not here - see .db_password

hypnotoad:
  listen:
    - http://127.0.0.1:3000
  pid_file: /home/tim2/provisioner/hypnotoad.pid
  workers: 2
  proxy: 1
